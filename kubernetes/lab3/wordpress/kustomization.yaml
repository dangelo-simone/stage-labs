apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - namespace.yaml
  - mariadb.yaml
  - wordpress.yaml

configMapGenerator:
  - name: filebeat
    namespace: wp1
    files:
      - filebeat.yml
