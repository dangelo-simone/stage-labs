installo Grafana con compose (uso bind mount stavolta) (mannaggia a me non avevo permessi di scrittura ecco perché continuava a riavviarsi)
localhost:3000 login: admin pass: admin
localhost:8080 login:admin pass:admin

cat compose.yml (quello del lab 1) >> docker-compose.yml (quello di Grafana) così ho lo stack nello stesso network

configuro data source su Grafana GUI (provisioning è di sicuro meglio):
	database: db:3306 (va usato nome del servizio, non localhost, altrimenti non lo trova nel network)
	db name, user, pass: quelli nel compose

query su Grafana funzionano-> db esiste e ha dati

con una query ho popolato la tabella dei post di wp così c'è qualcosa da monitorare

espando compose con il compose del tutorial di loki e con i file di config di loki e alloy

alloy prende i log tramite socket con docker, e loki li immagazzina